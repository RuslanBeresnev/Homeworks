#pragma once

#include <stdbool.h>

// Проверка работы функции на стандартной последовательности
bool standardSequenceTestPassed(void);

// Проверка работы функции на пустой последовательности
bool emptySequenceTestPassed(void);

// Проверка на прохождение всех тестов
bool generalTestPassed(void);